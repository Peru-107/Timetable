// Charts (Attendance's per-subject bar chart, Grades' course-grade bar
// chart) looked poor on a real phone in dark mode - cramped, low-contrast
// axis text, tooltip clipping. Flipped off app-wide rather than deleting the
// code, so they're a one-line change to bring back once reworked for mobile.
export const SHOW_CHARTS = false;
